<form method="post">
    @model Ecomm_project_1145_2.Model.CoverType
    @{
        ViewData["Title"] = "Upsert";
        var title = Model.Id != 0 ? "Edit CoverType" : "New CoverType";
       // var saveupdate = Model.Id != 0 ? "Update" : "Save";
    }
    <div class="row">
        <div class="col-10 border-bottom">
            <h2 class="text-primary">@title</h2>
        </div>
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="col-8 rounded border m-2 p-2">
            <div class="form-group row m-2 p-2">
                <div class="col-4">
                    <label asp-for="Name"></label>
                </div>
                <div class="col-8">
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row m-2 p-2">
                @*<div class="col-4 offset-4">
                    <button type="submit" class=" btn btn-info form-control">@saveupdate</button>
                </div>
                <div class="col-4">
                    <a asp-action="Index" class=" btn btn-success form-control">Back To List</a>
                </div>*@
                <div class="col-8 offset-4">
                    @if(Model.Id!=0)
                    {
                        <partial name="_EditAndBackToListButtons"/>
                    }
                    else
                    {
                        <partial name="_CreateAndBackToListButtons" />
                    }
                </div>
            </div>
        </div>

    </div>
</form>